/**
 * Page Projects
 * @description Affiche tous les projets via le composant AllProjects
 * @route /projects
 */

import AllProjects from "@/components/AllProjects";

export default function ProjectsPage() {
    return <AllProjects />;
}